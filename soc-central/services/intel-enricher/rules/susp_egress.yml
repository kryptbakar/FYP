title: Suspicious Outbound Connection to Common Reverse-Shell Port 4444
id: 6b1c2d3e-7a8b-4c5d-9e0f-112233445566
status: experimental
description: Detects outbound network flows to TCP/4444, the Metasploit/Meterpreter and
  reverse-shell default — a strong command-and-control indicator.
author: SOC Central
logsource:
  product: soc-central
  category: network_connection
detection:
  selection:
    payload.remote_port: 4444
  condition: selection
level: high
tags:
  - attack.command_and_control
  - attack.t1571
# Fallback OpenSearch query used if pySigma compilation is unavailable (intel-enricher reads this).
x_opensearch_query: "kind:network_flow AND payload.remote_port:4444"
