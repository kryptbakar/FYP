# Pinned to match the other services so the air-gap bundle carries one consistent set.
pydantic==2.10.4
psycopg[binary]==3.2.3
httpx==0.28.1

# The graph runtime and its Postgres checkpointer (resume after a restart).
langgraph==1.2.11
langgraph-checkpoint-postgres==3.1.2